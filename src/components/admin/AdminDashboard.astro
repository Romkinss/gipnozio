---
// AdminDashboard Component
// Главная страница админ-панели со статистикой

import { getCollection } from 'astro:content';

// Получаем данные
const blogPosts = await getCollection('blog');
const lessons = await getCollection('lessons');

// Вычисляем статистику
const stats = {
  totalPosts: blogPosts.length,
  totalLessons: lessons.length,
  recentPosts: blogPosts.filter(p => {
    const date = new Date(p.data.publishedAt);
    const sevenDaysAgo = new Date(Date.now() - 7 * 24 * 60 * 60 * 1000);
    return date > sevenDaysAgo;
  }).length,
  recentLessons: lessons.length > 0 ? Math.floor(lessons.length / 2) : 0,
};

// Последние посты
const recentPosts = blogPosts
  .sort((a, b) => new Date(b.data.publishedAt).getTime() - new Date(a.data.publishedAt).getTime())
  .slice(0, 5);
---

<div class="dashboard">
  <h1 class="page-title">📊 Главная панель</h1>
  <p class="page-description">Добро пожаловать в админ-панель ГИПНОЗИО</p>

  <!-- Статистика -->
  <div class="stats-grid">
    <div class="stat-card">
      <div class="stat-icon">📝</div>
      <div class="stat-content">
        <h3>Всего статей</h3>
        <p class="stat-number">{stats.totalPosts}</p>
        <p class="stat-subtitle">+{stats.recentPosts} за неделю</p>
      </div>
    </div>

    <div class="stat-card">
      <div class="stat-icon">📚</div>
      <div class="stat-content">
        <h3>Всего уроков</h3>
        <p class="stat-number">{stats.totalLessons}</p>
        <p class="stat-subtitle">+{stats.recentLessons} за неделю</p>
      </div>
    </div>

    <div class="stat-card">
      <div class="stat-icon">⭐</div>
      <div class="stat-content">
        <h3>Отзывы</h3>
        <p class="stat-number">0</p>
        <p class="stat-subtitle">Управление отзывами</p>
      </div>
    </div>

    <div class="stat-card">
      <div class="stat-icon">👥</div>
      <div class="stat-content">
        <h3>Пользователи</h3>
        <p class="stat-number">0</p>
        <p class="stat-subtitle">Активные пользователи</p>
      </div>
    </div>
  </div>

  <!-- Быстрые действия -->
  <div class="quick-actions">
    <h2>⚡ Быстрые действия</h2>
    <div class="actions-grid">
      <a href="/admin/blog/new" class="action-btn">
        <span class="action-icon">➕</span>
        <span>Новая статья</span>
      </a>
      <a href="/admin/lessons/new" class="action-btn">
        <span class="action-icon">➕</span>
        <span>Новый урок</span>
      </a>
      <a href="/admin/blog/" class="action-btn">
        <span class="action-icon">📋</span>
        <span>Управление статьями</span>
      </a>
      <a href="/admin/lessons/" class="action-btn">
        <span class="action-icon">📋</span>
        <span>Управление уроками</span>
      </a>
    </div>
  </div>

  <!-- Последние статьи -->
  <div class="recent-section">
    <h2>📰 Последние статьи</h2>
    {recentPosts.length > 0 ? (
      <div class="posts-list">
        {recentPosts.map((post) => (
          <div class="post-item">
            <div class="post-info">
              <h3>{post.data.title}</h3>
              <p class="post-meta">
                <span>Автор: {post.data.author}</span>
                <span>•</span>
                <span>{new Date(post.data.publishedAt).toLocaleDateString('ru-RU')}</span>
              </p>
            </div>
            <div class="post-actions">
              <a href={`/admin/blog/${post.slug}`} class="btn-small">Редактировать</a>
            </div>
          </div>
        ))}
      </div>
    ) : (
      <p class="empty-state">Нет статей. <a href="/admin/blog/new">Создать первую статью</a></p>
    )}
  </div>
</div>

<style>
  .dashboard {
    max-width: 1200px;
  }

  .page-title {
    font-size: 2rem;
    font-weight: 700;
    margin-bottom: 0.5rem;
    color: #1a1a1a;
  }

  .page-description {
    font-size: 1rem;
    color: #666;
    margin-bottom: 2rem;
  }

  .stats-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: 1.5rem;
    margin-bottom: 3rem;
  }

  .stat-card {
    background: white;
    border: 1px solid #e0e0e0;
    border-radius: 0.75rem;
    padding: 1.5rem;
    display: flex;
    gap: 1rem;
    box-shadow: 0 1px 2px rgba(0, 0, 0, 0.05);
    transition: all 0.3s ease;
  }

  .stat-card:hover {
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
    transform: translateY(-2px);
  }

  .stat-icon {
    font-size: 2.5rem;
    line-height: 1;
  }

  .stat-content h3 {
    font-size: 0.875rem;
    font-weight: 600;
    color: #666;
    margin-bottom: 0.5rem;
    text-transform: uppercase;
    letter-spacing: 0.5px;
  }

  .stat-number {
    font-size: 2rem;
    font-weight: 700;
    color: #D4AF37;
    margin-bottom: 0.25rem;
  }

  .stat-subtitle {
    font-size: 0.875rem;
    color: #999;
  }

  .quick-actions {
    background: white;
    border: 1px solid #e0e0e0;
    border-radius: 0.75rem;
    padding: 2rem;
    margin-bottom: 3rem;
    box-shadow: 0 1px 2px rgba(0, 0, 0, 0.05);
  }

  .quick-actions h2 {
    font-size: 1.25rem;
    font-weight: 600;
    margin-bottom: 1.5rem;
    color: #1a1a1a;
  }

  .actions-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
    gap: 1rem;
  }

  .action-btn {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    padding: 1rem;
    background: linear-gradient(135deg, #D4AF37 0%, #c9a227 100%);
    color: #1a1a1a;
    text-decoration: none;
    border-radius: 0.5rem;
    font-weight: 600;
    transition: all 0.3s ease;
    border: none;
    cursor: pointer;
  }

  .action-btn:hover {
    transform: translateY(-2px);
    box-shadow: 0 4px 12px rgba(212, 175, 55, 0.3);
  }

  .action-icon {
    font-size: 1.25rem;
  }

  .recent-section {
    background: white;
    border: 1px solid #e0e0e0;
    border-radius: 0.75rem;
    padding: 2rem;
    box-shadow: 0 1px 2px rgba(0, 0, 0, 0.05);
  }

  .recent-section h2 {
    font-size: 1.25rem;
    font-weight: 600;
    margin-bottom: 1.5rem;
    color: #1a1a1a;
  }

  .posts-list {
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  .post-item {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 1rem;
    border: 1px solid #f0f0f0;
    border-radius: 0.5rem;
    transition: all 0.3s ease;
  }

  .post-item:hover {
    background-color: #fafafa;
    border-color: #e0e0e0;
  }

  .post-info h3 {
    font-size: 1rem;
    font-weight: 600;
    color: #1a1a1a;
    margin-bottom: 0.5rem;
  }

  .post-meta {
    font-size: 0.875rem;
    color: #999;
    display: flex;
    gap: 0.5rem;
  }

  .post-actions {
    display: flex;
    gap: 0.5rem;
  }

  .btn-small {
    padding: 0.5rem 1rem;
    background-color: #D4AF37;
    color: #1a1a1a;
    text-decoration: none;
    border-radius: 0.375rem;
    font-size: 0.875rem;
    font-weight: 600;
    transition: all 0.3s ease;
    border: none;
    cursor: pointer;
  }

  .btn-small:hover {
    opacity: 0.9;
    transform: translateY(-1px);
  }

  .empty-state {
    text-align: center;
    padding: 2rem;
    color: #999;
  }

  .empty-state a {
    color: #D4AF37;
    text-decoration: none;
    font-weight: 600;
  }

  .empty-state a:hover {
    text-decoration: underline;
  }

  @media (max-width: 768px) {
    .stats-grid {
      grid-template-columns: 1fr;
    }

    .actions-grid {
      grid-template-columns: 1fr;
    }

    .post-item {
      flex-direction: column;
      align-items: flex-start;
      gap: 1rem;
    }

    .post-actions {
      width: 100%;
    }

    .btn-small {
      width: 100%;
      text-align: center;
    }
  }
</style>
