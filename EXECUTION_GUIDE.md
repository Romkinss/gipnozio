# 📋 ПОШАГОВАЯ ИНСТРУКЦИЯ ПО ВЫПОЛНЕНИЮ КАЖДОЙ ФАЗЫ

## ✅ ФАЗА 1: ПОДГОТОВКА И СТРУКТУРА (День 1-2)

### Шаг 1: Копирование шрифтов

**Команды:**
```bash
# Перейти в папку проекта
cd /workspace/gipnozio

# Создать папку для шрифтов
mkdir -p public/fonts

# Скопировать шрифты из React проекта
cp "/workspace/uploads/Для заливки для референса/public/fonts/"* public/fonts/

# Проверить, что шрифты скопированы
ls -la public/fonts/
```

**Ожидаемый результат:**
```
public/fonts/
├── inter-v18-cyrillic_latin-300.woff2
├── inter-v18-cyrillic_latin-regular.woff2
├── inter-v18-cyrillic_latin-500.woff2
├── inter-v18-cyrillic_latin-600.woff2
├── inter-v18-cyrillic_latin-700.woff2
├── pt-serif-v17-cyrillic_latin-regular.woff2
├── pt-serif-v17-cyrillic_latin-700.woff2
├── pt-sans-v17-cyrillic_latin-regular.woff2
└── pt-sans-v17-cyrillic_latin-700.woff2
```

**Визуальное подтверждение:**
- Скриншот: Список файлов в `public/fonts/`

---

### Шаг 2: Копирование логотипа и изображений

**Команды:**
```bash
# Создать папку для изображений
mkdir -p public/images

# Скопировать логотип
cp "/workspace/uploads/Для заливки для референса/public/logo-gipnozio.webp" public/images/

# Скопировать все остальные изображения
cp -r "/workspace/uploads/Для заливки для референса/public/"* public/images/ 2>/dev/null || true

# Проверить
ls -la public/images/
```

**Визуальное подтверждение:**
- Скриншот: Список файлов в `public/images/`

---

### Шаг 3: Обновление tailwind.config.mjs

**Действие:**
1. Открыть файл `/workspace/gipnozio/tailwind.config.mjs`
2. Заменить содержимое на код из `COMPONENT_EXAMPLES.md` (раздел 1.1)
3. Сохранить файл

**Проверка:**
```bash
# Проверить синтаксис
cat tailwind.config.mjs | grep -A 5 "colors:"
```

**Ожидаемый результат:**
```
colors: {
  gold: {
    DEFAULT: '#D4AF37',
    light: '#F4E5B0',
    dark: '#B8941F',
    dim: 'rgba(212, 175, 55, 0.1)',
  },
```

**Визуальное подтверждение:**
- Скриншот: Содержимое `tailwind.config.mjs` с правильными цветами

---

### Шаг 4: Обновление src/globals.css

**Действие:**
1. Открыть файл `/workspace/gipnozio/src/globals.css`
2. Заменить содержимое на код из `COMPONENT_EXAMPLES.md` (раздел 1.2)
3. Сохранить файл

**Проверка:**
```bash
# Проверить, что CSS переменные добавлены
grep -n "color-gold-primary" src/globals.css
```

**Ожидаемый результат:**
```
--color-gold-primary: #D4AF37;
--color-gold-light: #F4E5B0;
--color-gold-dark: #B8941F;
```

**Визуальное подтверждение:**
- Скриншот: Содержимое `src/globals.css` с CSS переменными

---

### Шаг 5: Проверка в браузере

**Действие:**
1. Открыть браузер
2. Перейти на `https://4321-ba3d9962-b45d-4b61-acd6-6d7805def792.proxy.daytona.works`
3. Проверить, что шрифты загружены (откройте DevTools → Network → Fonts)
4. Проверить, что цвета применены (откройте DevTools → Elements → Inspect)

**Ожидаемый результат:**
- Шрифты загружены без ошибок
- Цвета применены правильно
- Нет красных ошибок в консоли

**Визуальное подтверждение:**
- Скриншот: DevTools → Network (шрифты загружены)
- Скриншот: DevTools → Console (нет ошибок)

---

### Шаг 6: Git коммит

**Команды:**
```bash
# Проверить изменения
git status

# Добавить все файлы
git add .

# Сделать коммит
git commit -m "Phase 1: Setup fonts, images, and Tailwind configuration"

# Проверить коммит
git log --oneline -1
```

**Ожидаемый результат:**
```
Phase 1: Setup fonts, images, and Tailwind configuration
```

**Визуальное подтверждение:**
- Скриншот: Результат `git log --oneline -1`

---

## ✅ ФАЗА 2: LAYOUT И БАЗОВЫЕ КОМПОНЕНТЫ (День 3-4)

### Шаг 1: Создание MainLayout.astro

**Действие:**
1. Создать файл `/workspace/gipnozio/src/layouts/MainLayout.astro`
2. Добавить код из `COMPONENT_EXAMPLES.md` (раздел 2.1)
3. Сохранить файл

**Проверка:**
```bash
# Проверить, что файл создан
ls -la src/layouts/MainLayout.astro
```

**Визуальное подтверждение:**
- Скриншот: Содержимое `src/layouts/MainLayout.astro`

---

### Шаг 2: Создание Navbar.astro

**Действие:**
1. Создать файл `/workspace/gipnozio/src/components/Navbar.astro`
2. Добавить код из `COMPONENT_EXAMPLES.md` (раздел 2.2)
3. Сохранить файл

**Проверка:**
```bash
# Проверить, что файл создан
ls -la src/components/Navbar.astro
```

**Визуальное подтверждение:**
- Скриншот: Navbar на десктопе (1920px)
- Скриншот: Navbar на мобильном (375px)

---

### Шаг 3: Создание Footer.astro

**Действие:**
1. Создать файл `/workspace/gipnozio/src/components/Footer.astro`
2. Добавить код из `COMPONENT_EXAMPLES.md` (раздел 2.3)
3. Сохранить файл

**Проверка:**
```bash
# Проверить, что файл создан
ls -la src/components/Footer.astro
```

**Визуальное подтверждение:**
- Скриншот: Footer на десктопе
- Скриншот: Footer на мобильном

---

### Шаг 4: Обновление главной страницы

**Действие:**
1. Открыть файл `/workspace/gipnozio/src/pages/index.astro`
2. Обновить, чтобы использовать новый MainLayout
3. Добавить Navbar и Footer

**Код:**
```astro
---
import MainLayout from '../layouts/MainLayout.astro';
import Navbar from '../components/Navbar.astro';
import Footer from '../components/Footer.astro';
---

<MainLayout title="Eurasian Academy of Hypnosis">
  <Navbar />
  <main>
    <!-- Контент будет добавлен в следующих фазах -->
  </main>
  <Footer />
</MainLayout>
```

**Визуальное подтверждение:**
- Скриншот: Главная страница с Navbar и Footer

---

### Шаг 5: Git коммит

**Команды:**
```bash
git add .
git commit -m "Phase 2: Create MainLayout, Navbar, and Footer components"
git log --oneline -1
```

**Визуальное подтверждение:**
- Скриншот: Результат `git log --oneline -1`

---

## ✅ ФАЗА 3: ГЛАВНАЯ СТРАНИЦА - HERO СЕКЦИЯ (День 5)

### Шаг 1: Создание Hero.tsx

**Действие:**
1. Создать файл `/workspace/gipnozio/src/components/Hero.tsx`
2. Добавить код из `COMPONENT_EXAMPLES.md` (раздел 3.1)
3. Сохранить файл

**Проверка:**
```bash
# Проверить, что файл создан
ls -la src/components/Hero.tsx
```

**Визуальное подтверждение:**
- Скриншот: Hero на десктопе (1920px)
- Скриншот: Hero на планшете (768px)
- Скриншот: Hero на мобильном (375px)
- Скриншот: Hover эффект на кнопке "НАЧАТЬ ПРАКТИКУ"

---

### Шаг 2: Добавление Hero на главную страницу

**Действие:**
1. Открыть файл `/workspace/gipnozio/src/pages/index.astro`
2. Импортировать Hero компонент
3. Добавить Hero в разметку

**Код:**
```astro
---
import MainLayout from '../layouts/MainLayout.astro';
import Hero from '../components/Hero';
---

<MainLayout title="Eurasian Academy of Hypnosis">
  <Hero client:load />
</MainLayout>
```

**Визуальное подтверждение:**
- Скриншот: Главная страница с Hero секцией

---

### Шаг 3: Проверка анимаций

**Действие:**
1. Открыть браузер
2. Перейти на главную страницу
3. Проверить, что все элементы появляются с анимацией fade-in-up
4. Проверить, что кнопки имеют hover эффекты

**Ожидаемый результат:**
- Элементы появляются с задержкой (0s, 0.2s, 0.4s, 0.6s, 0.8s)
- Кнопки поднимаются при наведении
- Кнопки сжимаются при клике

**Визуальное подтверждение:**
- Скриншот: Hero с анимациями
- Видео: Hover эффект на кнопке

---

### Шаг 4: Git коммит

**Команды:**
```bash
git add .
git commit -m "Phase 3: Add Hero component to landing page"
git log --oneline -1
```

**Визуальное подтверждение:**
- Скриншот: Результат `git log --oneline -1`

---

## ✅ ФАЗА 4: ГЛАВНАЯ СТРАНИЦА - ОСТАЛЬНЫЕ СЕКЦИИ (День 6-7)

### Шаг 1: Создание AcademyPulse.tsx

**Действие:**
1. Создать файл `/workspace/gipnozio/src/components/AcademyPulse.tsx`
2. Добавить код из `COMPONENT_EXAMPLES.md` (раздел 4.1)
3. Сохранить файл

**Визуальное подтверждение:**
- Скриншот: AcademyPulse компонент

---

### Шаг 2: Создание остальных компонентов

**Действие:**
1. Создать файлы для каждого компонента:
   - `src/components/Resonance.tsx`
   - `src/components/Problem.tsx`
   - `src/components/Solution.tsx`
   - `src/components/DigitalManifesto.tsx`
   - `src/components/GlobalCareer.tsx`
   - `src/components/Stats.tsx`
   - `src/components/ForWhom.tsx`
   - `src/components/Testimonials.tsx`
   - `src/components/Program.tsx`
   - `src/components/Scarcity.tsx`
   - `src/components/Guarantee.tsx`
   - `src/components/CallToAction.tsx`
   - `src/components/Warning.tsx`

2. Для каждого компонента:
   - Добавить правильную структуру HTML
   - Добавить правильные классы Tailwind
   - Добавить правильные цвета и шрифты
   - Добавить анимации

**Визуальное подтверждение:**
- Скриншот: Каждый компонент на странице

---

### Шаг 3: Добавление всех компонентов на главную страницу

**Действие:**
1. Открыть файл `/workspace/gipnozio/src/pages/index.astro`
2. Импортировать все компоненты
3. Добавить их в правильном порядке

**Код:**
```astro
---
import MainLayout from '../layouts/MainLayout.astro';
import Hero from '../components/Hero';
import AcademyPulse from '../components/AcademyPulse';
import Resonance from '../components/Resonance';
import Problem from '../components/Problem';
import Solution from '../components/Solution';
import DigitalManifesto from '../components/DigitalManifesto';
import GlobalCareer from '../components/GlobalCareer';
import Stats from '../components/Stats';
import ForWhom from '../components/ForWhom';
import Testimonials from '../components/Testimonials';
import Program from '../components/Program';
import Scarcity from '../components/Scarcity';
import Guarantee from '../components/Guarantee';
import CallToAction from '../components/CallToAction';
import Warning from '../components/Warning';
---

<MainLayout title="Eurasian Academy of Hypnosis">
  <Hero client:load />
  <AcademyPulse client:load />
  <Resonance client:load />
  <Problem client:load />
  <Solution client:load />
  <DigitalManifesto client:load />
  <GlobalCareer client:load />
  <Stats client:load />
  <ForWhom client:load />
  <Testimonials client:load />
  <Program client:load />
  <Scarcity client:load />
  <Guarantee client:load />
  <CallToAction client:load />
  <Warning client:load />
</MainLayout>
```

**Визуальное подтверждение:**
- Скриншот: Полная главная страница на десктопе
- Скриншот: Полная главная страница на мобильном (прокрутка вверх)
- Скриншот: Полная главная страница на мобильном (прокрутка вниз)

---

### Шаг 4: Git коммит

**Команды:**
```bash
git add .
git commit -m "Phase 4: Add all landing page sections (AcademyPulse, Resonance, Problem, Solution, etc.)"
git log --oneline -1
```

**Визуальное подтверждение:**
- Скриншот: Результат `git log --oneline -1`

---

## 📝 ФАЗЫ 5-11: ОСТАЛЬНЫЕ СТРАНИЦЫ

Для каждой фазы (5-11) следуйте этому же шаблону:

1. **Создание компонентов** - создайте все необходимые компоненты
2. **Создание страниц** - создайте страницы в `src/pages/`
3. **Добавление контента** - добавьте контент из React проекта
4. **Проверка в браузере** - проверьте, что всё отображается правильно
5. **Git коммит** - сделайте коммит с описанием фазы

---

## 🎯 ФИНАЛЬНАЯ ПРОВЕРКА

После завершения всех фаз:

1. **Проверить все страницы:**
   ```bash
   # Проверить, что все страницы существуют
   find src/pages -name "*.astro" -o -name "*.tsx" | sort
   ```

2. **Проверить все компоненты:**
   ```bash
   # Проверить, что все компоненты существуют
   find src/components -name "*.tsx" -o -name "*.astro" | sort
   ```

3. **Проверить производительность:**
   ```bash
   # Открыть DevTools → Lighthouse
   # Проверить, что все метрики выше 90
   ```

4. **Проверить SEO:**
   ```bash
   # Открыть DevTools → Lighthouse → SEO
   # Проверить, что SEO score выше 95
   ```

5. **Финальный коммит:**
   ```bash
   git add .
   git commit -m "Final: Complete migration from React to Astro - all pages and components implemented"
   git log --oneline -5
   ```

---

## 📊 ЧЕКЛИСТ ЗАВЕРШЕНИЯ

- [ ] Фаза 1: Подготовка и структура ✅
- [ ] Фаза 2: Layout и базовые компоненты ✅
- [ ] Фаза 3: Главная страница - Hero ✅
- [ ] Фаза 4: Главная страница - остальное ✅
- [ ] Фаза 5: Страница блога ✅
- [ ] Фаза 6: Страница клиентов ✅
- [ ] Фаза 7: Страница входа ✅
- [ ] Фаза 8: Страница обучения ✅
- [ ] Фаза 9: Страница отзывов ✅
- [ ] Фаза 10: Страница автора ✅
- [ ] Фаза 11: Интеграция и оптимизация ✅
- [ ] Все страницы проверены на десктопе ✅
- [ ] Все страницы проверены на мобильном ✅
- [ ] Lighthouse score > 90 ✅
- [ ] SEO score > 95 ✅
- [ ] Все ссылки работают ✅
- [ ] Все кнопки работают ✅
- [ ] Все анимации работают ✅
- [ ] Git история чистая ✅

---

**Дата создания:** 27 декабря 2025  
**Версия:** 1.0  
**Статус:** Готово к использованию
